#include <stdio.h>

int main()
{
    for (int x = 0; x < 2; x++)
    {
        printf("%d\n", x);
    }

    for(int x = 0; x < 2; x++)   {
        printf("%d\n", x);
    }

    for
        (int x = 0; x < 2; x++)

    {
        printf("%d\n", x);
    }

    return 0;
}
